// make sure build is run in sudo mode
fn main() {
    // run server on specified port

    // on requests, stop systemctl service

    // pull latest query file from s3

    // run lint and compile queries

    // start systemctl service
}
